HTML Tasks:
Dynamic Content:
Use HTML and CSS to create a layout for a portfolio website.
Add an area in the <body> for dynamically loaded content using JavaScript.

HTML Audio/Video:
Embed a video using the <video> tag and a song using the <audio> tag.
Add controls for playback, and autoplay the media.


CSS tasks: 
Animations:
Use @keyframes to create a bounce animation for a button on hover.


JavaScript Tasks:
Fetch API:
Use the fetch() function to get data from a public API (like the JSONPlaceholder API) and display the data in your HTML.
